"""
Take in data parameter, data augmentation strategy, network hyper-parameter,
and generated nested cv test score (mean +- std)
"""

def tripnet_nest_cv():

    # read data frame with protocol information
    # Build the network according to hyper-parameter
    # train and test set according to cv
    # Perform data augmentation according to data-set type and data augmentation strategy, obtain train, validation set
    # Obtain the trained model and corresponding test accuracy (mean +- std)

    return